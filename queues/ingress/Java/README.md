# ingress — Java

Queue ingress bridge — TriggerEvent and RunBridge via cawbsingress

```shell
cd queues/ingress/Java && mvn -q compile
```

Requires **cawbs** Java jar on classpath (`mvn install` in `cawbs/Java`).

## License

Apache License 2.0 — see [LICENSE](../../LICENSE).

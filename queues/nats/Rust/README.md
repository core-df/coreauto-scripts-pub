# nats — Rust

**Publish** from steps; **Subscribe** for ingress bridges.

See [Python](../Python/README.md) for API reference and environment variables.

Crate: `async-nats` with tokio `block_on`. Build with `cargo build` in this directory.

Apache License 2.0.

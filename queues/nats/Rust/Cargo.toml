[package]
name = "coreauto-nats"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "NATS helpers for Core Auto step scripts"

[dependencies]
async-nats = "0.33"
futures = "0.3"
serde_json = "1"
tokio = { version = "1", features = ["rt", "time", "macros"] }

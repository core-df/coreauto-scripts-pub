# sqs — Rust

**Send** from steps; **Receive** for ingress bridges.

See [Python](../Python/README.md) for API reference and environment variables.

Crate: `aws-sdk-sqs` with tokio `block_on`. Build with `cargo build` in this directory.

Apache License 2.0.

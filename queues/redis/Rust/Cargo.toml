[package]
name = "coreauto-redis"
version = "1.0.0"
edition = "2021"
license = "Apache-2.0"
description = "Redis list helpers for Core Auto step scripts"

[dependencies]
redis = { version = "0.25", features = ["connection-manager"] }
serde_json = "1"
urlencoding = "2"
